# === Application ===
APP_PORT=8080
APP_ENV=development

# === Database (PostgreSQL) ===
DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_PASSWORD=
DB_NAME=ecommerce_cms
DB_SSLMODE=disable

# === Auth ===
# Wajib diisi. Jangan pakai default. Generate: openssl rand -base64 32
JWT_SECRET=
